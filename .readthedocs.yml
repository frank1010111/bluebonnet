# Required
version: 2

# Image to use
build:
  os: ubuntu-20.04
  tools:
    python: "3.9"

# Configuration
python:
  install:
    - method: pip
      path: .[docs]

# Build documentation in the docs/ directory with Sphinx
sphinx:
  configuration: docs/conf.py
